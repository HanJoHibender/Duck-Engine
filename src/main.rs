use ::duck_window::window::{self, duck_window};
pub use duck_internal::*;

fn main() {
    duck_window::init(&mut self::duck_window {
        title: "Hello",
        width: 300,
        height: 100,
    });
}
