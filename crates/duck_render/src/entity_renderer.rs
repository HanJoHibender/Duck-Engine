use std::collections::HashMap;
use std::sync::Arc;
use duck_scene::duck_component::Drawable::Drawable;


//static drawables: HashMap<u128, Drawable> = HashMap::new();

pub fn render_entities(){

}

pub fn add_drawable(drawable:Drawable){
    //drawables.insert(drawable)
}