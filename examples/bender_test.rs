use duck_internal::bender_window::window::simple_window;

fn main() {
    // spawn window
    simple_window();

    // innitialise render
    // window.insert_render();
    // generate World
    // visualise
}
