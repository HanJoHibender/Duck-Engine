use duck_internal::duck_window::window::duck_window;

fn main() {

    println!("{}", duck_internal::duck_math::generate_uuid());
    println!("{}", duck_internal::duck_math::generate_uuid());
    println!("{}", duck_internal::duck_math::generate_uuid());

    duck_window::init(&mut self::duck_window {
        title: "Hello",
        width: 1920,
        height: 1080,
    });
}
